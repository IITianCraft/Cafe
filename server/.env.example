# Server Configuration
PORT=5000
NODE_ENV=development
# FRONTEND_ORIGIN should be the URL of your frontend (e.g., https://nourish-admin-dash.lovable.app or http://localhost:8080)
FRONTEND_ORIGIN=http://localhost:8080

# Firebase Admin Configuration
# These values can be found in your Firebase Project Settings > Service Accounts
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=your-client-email@your-project-id.iam.gserviceaccount.com
# IMPORTANT: For private key, ensure newlines are handled correctly.
# If copying from JSON file, preserve the \n characters.
# In local .env, it might look like "-----BEGIN PRIVATE KEY-----\n..."
# In some cloud providers (like Railway), you might need to use the raw value or replace \n with actual newlines.
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----"

# Firebase Client Configuration (for frontend reference mostly, but useful to keep together)
FIREBASE_API_KEY=your-api-key
FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
FIREBASE_STORAGE_BUCKET=your-project-id.firebasestorage.app
